
过渡：让元素的状态逐渐的发生变化
创建过度的步骤：
1：布局页面
2：触发修改属性
3：给修改属性添加过渡效果
过渡的属性：
1、transition-delay: 时间；延迟过渡的执行,时间可以是秒或者毫秒
2、transition-timing-function:linear/ease默认的/ease-in/ease-out/ease-in-out/cubic-bezier(x1,y1,x2,y2);过渡的执行速度 
3、transition:需要过渡的属性 执行过渡的时间 执行过渡的速度 延迟时间;/transition:width 3s linear 2s;
4、transition:需要过渡的属性 执行过渡的时间 执行过渡的速度 延迟时间,需要过渡的属性 执行过渡的时间 执行过渡的速度 延迟时间...;/transition:width 3s linear 2s,background-color 3s linear 2s;
5、多个属性发生同样的过渡效果简写/把多个属性简写为all/transition:all 3s;
6、

2D转换： 使元素在平面上改变形状、尺寸和位置的一种效果。
transform一共有3个取值，分别为：rotate(ndeg)、translate、scale。
旋转：格式为: transform: rotate(ndeg);/n是一个具体的数字,deg代表度数的单位
平移：格式:transform:translate(水平方向移动距离,垂直方向移动距离);/移动距离可以是负数,负数代表向反反向移动。
缩放：格式: transform: scale(水平方向缩放比例,垂直方向缩放比例);/缩放比例如果是1代表不变,大于1代表放大,小于1代表缩小/如果水平和垂直方向上缩放比例一样,可以简写一个。例如：transform:scale(1.2);缩放为1，2倍。
复合样式：格式: transform: rotate(ndeg) translate(水平方向移动距离,垂直方向移动距离) scale(水平方向缩放比例,垂直方向缩放比例);/多个转换之间用空格隔开/旋转会修改坐标系,因此旋转后再平移(即旋转写在平移前面)是在旋转修改后的坐标系下平移。
修改旋转基点：格式: transform-origin: 水平方向移动距离 垂直方向移动距离;/旋转基点就是以哪个点为参考点进行旋转/ 默认情况下所有的元素都是以自己的中心点作为参考来旋转的/移动距离可以是具体的像素,百分比,和关键字,关键字在水平方向上有left center right,在垂直方向上有top center bottom/移动基点的参考点是元素的左上角顶点。
例如：transform-origin: center bottom; 修改为：中间下部旋转
修改旋转轴向：在rotate后面加上轴向可以改变旋转的轴向,即rotateZ是沿着Z轴旋转,rotateX是沿着X轴旋转,rotateY是沿着Y轴旋转。/默认情况下元素是沿着Z轴旋转的,即rotateZ和rotate是等价的。/
为了让元素呈现近大远小效果,需要给元素的父元素上面添加透视距离属性perspective：为值像素，单位为px。
动画：
创建的步骤：
1：给需要执行动画的元素指定动画的名称
2：创建指定名称的动画
3：给需要执行动画的元素指定动画的执行时间
1、用百分比创建动画：0%代表动画开始,100%代表动画结束，0%和100%推荐都写。
2、旋转动画格式写法: transform: rotate(ndeg);/n是一个具体的数字,deg代表度数的单位。
3、平移动画格式写法: transform: translate(水平方向移动距离,垂直方向移动距离);/移动距离可以是负数,负数代表向反反向移动
4、缩放动画格式写法:transform:scale(1.2)；/缩放比例如果是1代表不变,大于1代表放大,小于1代表缩小/如果水平和垂直方向上缩放比例一样,可以简写一个。

1.	过渡和动画之间的异同
1.1不同点
          过渡必须人为的触发才会执行动画
          动画不需要人为的触发就可以执行动画
1.2相同点
          过渡和动画都是用来给元素添加动画的
          过渡和动画都是系统新增的一些属性
          过渡和动画都需要满足三要素才会有动画效果



